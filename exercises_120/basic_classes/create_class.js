// Create an empty class named Cat.

class Cat {

} 

/*
When we wnat to create a `class` we use `class` keyword followed by the name of the class which is in this case `Cat`, and then a pair of curly braces (body of the class)
*/